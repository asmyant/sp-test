{% macro radio(props) %}
   <div class="form-radio">
      <input type="radio" class="form-radio__input" id="{{ props.for }}" {% if props.checked %}checked{% endif %} name="{{ props.name }}">
      <label for="{{ props.for }}" class="form-radio__label">{{ props.title }}</label>
   </div>
{% endmacro %}